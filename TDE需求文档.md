# UDB-TX 透明数据加密（TDE）需求文档

| 项目 | 内容 |
|---|---|
| 文档状态 | 草案（V0.1） |
| 目标版本 | 待定 |
| 适用产品 | UDB-TX 数据库内核 |
| 功能类型 | 数据静态加密（Data-at-Rest Encryption） |
| 加密粒度 | V1：数据库级启用、物理页级加密 |
| 文档日期 | 2026-07-20 |

## 1. 背景

数据库数据文件、预写日志、临时文件及备份介质可能因磁盘丢失、介质复制、未授权文件访问或备份泄露而暴露数据。现有访问控制、网络加密和审计机制不能阻止攻击者绕过数据库服务直接读取静态文件。

UDB-TX 需要提供透明数据加密（Transparent Data Encryption，TDE）能力，在不改变应用 SQL、表结构、数据类型及查询语义的前提下，对落盘数据进行实时加密，并在数据库读取时自动解密。

## 2. 目标

TDE 应实现以下目标：

1. 数据页写入持久化介质前被加密，从持久化介质读取后在内存中解密。
2. 对应用、SQL、执行计划、索引语义、事务语义和客户端协议保持透明。
3. 数据文件、索引文件、TOAST、WAL、临时文件及物理备份中不得出现受保护页面的明文内容。
4. 使用分层密钥体系，支持外部密钥管理服务，并支持安全的密钥轮换、备份与恢复。
5. 保持崩溃恢复、流复制、归档恢复、时间点恢复和物理备份的正确性。
6. 提供可观测、可审计、可运维的启停、迁移和轮换能力。

## 3. 非目标

V1 不解决以下问题：

1. 不阻止拥有合法查询权限的用户、数据库超级用户或已控制数据库进程的攻击者读取明文。
2. 不替代 TLS、访问控制、审计、操作系统加固、磁盘加密和备份访问控制。
3. 不提供按列授权解密、客户端持钥或“数据库管理员不可见”的列级加密能力。
4. 不对 SQL 文本、客户端结果、数据库日志中的业务参数自动脱敏。
5. 不保证 core dump、操作系统 swap、休眠镜像及第三方工具生成文件中的明文安全；这些场景由部署安全规范覆盖。
6. V1 不要求在线更换数据加密密钥并立即重写全部历史页面；该能力可在后续版本实现。

## 4. 术语

| 术语 | 说明 |
|---|---|
| TDE | 透明数据加密，保护数据库静态数据 |
| KEK | Key Encryption Key，密钥加密密钥，用于包装 DEK |
| DEK | Data Encryption Key，数据加密密钥，用于加密数据页 |
| WAL DEK | 专用于 WAL 的数据加密密钥 |
| Key Provider | 提供 KEK 保存、获取、包装和解包能力的密钥管理组件 |
| Key ID | 密钥的稳定逻辑标识 |
| Key Version | 密钥版本，用于轮换和历史数据读取 |
| Envelope Encryption | 使用 KEK 包装 DEK、使用 DEK 加密数据的信封加密体系 |
| 加密域 | 共享同一 DEK 和策略的一组物理文件 |

## 5. 安全模型

### 5.1 保护对象

TDE 必须防护以下离线攻击场景：

- 数据目录或单个关系文件被复制；
- 数据盘、云盘快照或持久卷泄露；
- WAL 文件或归档 WAL 泄露；
- 临时文件被恢复或复制；
- 物理基础备份及其增量数据泄露；
- 未持有有效 KEK 的环境尝试启动、挂载或恢复加密数据库。

### 5.2 信任边界

以下组件位于可信边界内：

- 正常运行的 UDB-TX 服务进程；
- Shared Buffer 及数据库进程私有内存；
- 经认证授权的 Key Provider；
- 获得数据库授权的合法查询用户。

以下对象位于可信边界外：

- 数据文件、WAL、归档、临时文件和备份介质；
- 未经授权复制到其他主机的数据目录；
- 不持有 KEK 的恢复环境。

### 5.3 安全原则

- KEK 不得直接用于页面加密。
- 明文 KEK 不得持久化在数据库数据目录或配置文件中。
- 明文 DEK 不得写入数据库文件、WAL、日志、core 文件或错误消息。
- 不同用途应使用不同 DEK，至少区分数据文件、WAL 和临时文件。
- 密钥材料在内存中应尽量缩短生命周期，并在释放时清零。
- 数据加密不得依赖可重复使用且不安全的 nonce/IV 组合。
- 密钥缺失、认证失败或页面完整性失败时必须失败关闭，不得回退为明文读取或写入。

## 6. 总体功能范围

### 6.1 V1 必须覆盖

| 对象 | V1 要求 |
|---|---|
| Heap 主数据文件 | 必须加密 |
| 所有索引文件 | 必须加密 |
| TOAST 表及其索引 | 必须加密 |
| FSM、VM、init fork | 必须加密 |
| WAL 在线文件 | 必须加密 |
| 归档 WAL | 必须保持密文 |
| 排序、Hash、物化等临时文件 | 必须加密 |
| 临时表物理文件 | 必须加密 |
| 物理基础备份 | 必须保持密文，且携带恢复所需的非敏感密钥元数据 |
| 流复制传输的物理 WAL | 必须保持密文或由加密传输通道保护；落盘后必须为密文 |

### 6.2 V1 可选覆盖

- 逻辑解码 spill 文件；
- 统计子系统产生的持久化临时文件；
- 服务端扩展或插件自行创建的文件；
- 逻辑备份文件；
- 服务端 `COPY TO` 导出的业务文件。

可选对象未覆盖时，产品文档和状态查询必须明确显示限制。

## 7. 功能需求

### 7.1 启用与禁用

**TDE-FR-001** 系统应支持在初始化新实例时设置默认 TDE 策略。

**TDE-FR-002** 系统应支持为数据库启用或禁用 TDE。V1 的 SQL 或管理接口形式可在概要设计中确定。

**TDE-FR-003** 新建数据库应支持继承实例默认策略或模板数据库策略。

**TDE-FR-004** 对已有数据库启用 TDE 时，系统必须启动后台加密扫描，将已有明文页面转换为密文。

**TDE-FR-005** 后台加密或解密扫描必须支持进度查询、暂停、继续和故障后恢复。

**TDE-FR-006** 转换期间必须允许正常事务读写；新写出的目标页面必须遵循当前转换策略。

**TDE-FR-007** 在数据库仍存在由某版本密钥加密的页面、WAL 或备份依赖时，不得删除相应密钥版本。

### 7.2 数据页加解密

**TDE-FR-010** Shared Buffer 中的数据页保持明文，写盘时必须使用独立 I/O 缓冲区生成密文，不得原地加密共享页面。

**TDE-FR-011** 磁盘读入的加密页必须在进入 Shared Buffer 或被上层解析前完成解密与校验。

**TDE-FR-012** 加密不得改变数据库逻辑页大小，不得改变 SQL 可见的数据类型、长度语义及索引行为。

**TDE-FR-013** 每个加密页必须绑定其物理身份，至少包含加密域、关系文件标识、fork、块号和密钥版本，防止页面被无检测地移动到其他位置。

**TDE-FR-014** 加密页面必须支持判断算法、格式版本和 Key Version。

**TDE-FR-015** 解密失败、密钥不存在、页面认证失败或格式不支持时，系统必须报告明确错误并停止使用该页面。

**TDE-FR-016** TDE 必须与数据库页面校验和兼容；校验和的计算、加密、解密和验证顺序必须固定并记录在设计文档中。

### 7.3 WAL 加密

**TDE-FR-020** WAL 应使用独立于数据文件的 WAL DEK。

**TDE-FR-021** WAL 在写入 `pg_wal` 和归档目标前必须为密文。

**TDE-FR-022** WAL Reader 必须在记录解析和 Redo 前完成解密与完整性校验。

**TDE-FR-023** WAL 加密必须兼容崩溃恢复、归档恢复、PITR、流复制、同步复制和级联复制。

**TDE-FR-024** WAL 密钥轮换不得产生主库可写但备库无法继续重放的时间窗口。

**TDE-FR-025** 密钥切换记录必须具备明确顺序和持久化语义，使崩溃恢复可以确定每个 WAL 段所用的 Key Version。

### 7.4 临时数据加密

**TDE-FR-030** TDE 启用后，由执行器生成的排序、Hash、Materialize 等临时文件必须加密。

**TDE-FR-031** 临时文件密钥应与长期数据 DEK 分离，可在实例启动时生成；实例正常退出后无需用于恢复的临时密钥不得持久化。

**TDE-FR-032** 临时文件异常遗留时，重启后的数据库应能安全删除，不得因缺少临时密钥而阻塞启动。

### 7.5 密钥管理

**TDE-FR-040** 系统必须采用 KEK 包装 DEK 的两级或多级密钥体系。

**TDE-FR-041** Key Provider 应使用可插拔接口，至少支持一种生产级 Provider；可选实现包括本地加密 Wallet、KMIP、云 KMS 或 HSM。

**TDE-FR-042** 数据目录中只能保存被 KEK 包装后的 DEK 及非敏感元数据，不得保存明文 KEK、明文 DEK 或可直接恢复它们的凭据。

**TDE-FR-043** Provider 接口至少应支持：初始化、健康检查、包装、解包、创建密钥、查询 Key Version 和轮换。

**TDE-FR-044** 系统启动时无法获取必要密钥，应拒绝打开对应加密数据库，并输出不包含密钥材料的诊断信息。

**TDE-FR-045** 解包后的 DEK 可以在数据库进程内安全缓存；缓存应具备并发安全、超时或生命周期管理以及安全清零机制。

**TDE-FR-046** 密钥和 Provider 凭据不得出现在 SQL 日志、错误日志、审计详情、进程标题、命令行参数或状态视图中。

### 7.6 密钥轮换

**TDE-FR-050** 系统必须支持 KEK 轮换。KEK 轮换应仅重新包装 DEK，不重写全部数据页。

**TDE-FR-051** 系统应保留恢复历史备份和历史 WAL 所需的历史 KEK/DEK 版本引用关系。

**TDE-FR-052** 系统应提供 DEK 轮换能力；若 V1 不实现全量页面重加密，必须支持创建新版本并让新写页面使用新 DEK，同时保留旧 DEK 的读取能力。

**TDE-FR-053** DEK 后台重加密应可查询进度、暂停、继续、限速，并在崩溃后恢复。

**TDE-FR-054** 轮换操作必须记录操作者、时间、对象、旧版本、新版本、结果和失败原因，但不得记录密钥内容。

### 7.7 备份、恢复与复制

**TDE-FR-060** `pg_basebackup` 或对应物理备份工具必须能够备份 TDE 数据库，备份中的数据页面保持密文。

**TDE-FR-061** 备份清单应记录恢复所需的加密格式版本、Key ID、Key Version 和 Provider 类型，但不得包含明文密钥。

**TDE-FR-062** 恢复工具必须在正式恢复前提供密钥可用性预检查，避免写入大量数据后才发现密钥缺失。

**TDE-FR-063** 加密集群的主备节点必须具有一致的密钥可用性；备库缺少新密钥时，主库的密钥切换操作必须失败或等待，具体策略在概要设计中确定。

**TDE-FR-064** 物理复制不得要求在发送端把数据页或 WAL 解密后再传输。

**TDE-FR-065** 逻辑复制保持现有逻辑语义，发布端可在数据库内存中读取明文；传输安全由 TLS 和复制权限保证，订阅端是否启用 TDE 由订阅端策略决定。

### 7.8 监控与审计

**TDE-FR-070** 系统必须提供 TDE 状态视图或等价接口，至少展示：

- 数据库及加密域是否启用；
- 当前状态；
- 算法和格式版本；
- 当前 Key ID 与 Key Version；
- 加密/解密/重加密扫描进度；
- 最近一次轮换时间；
- Provider 健康状态；
- 最后一次错误的非敏感摘要。

**TDE-FR-071** TDE 状态至少应包含：

```text
UNENCRYPTED
ENCRYPTING
ENCRYPTED
KEY_ROTATING
DECRYPTING
SUSPENDED
ERROR
```

**TDE-FR-072** 启用、禁用、暂停、继续、轮换、导入或删除密钥等管理操作必须写入审计日志。

**TDE-FR-073** 应提供页加密/解密次数、字节数、耗时、失败数、密钥缓存命中率和 Provider 调用耗时等统计指标。

### 7.9 权限管理

**TDE-FR-080** TDE 管理权限必须与普通数据库管理权限分离，支持授予专用系统权限或预定义角色。

**TDE-FR-081** 查询 TDE 基本状态与查询敏感密钥元数据应使用不同权限。

**TDE-FR-082** 数据库超级用户默认不得直接读取明文密钥材料。

**TDE-FR-083** Provider 凭据的文件权限、属主和访问方式必须在安装检查及启动检查中验证。

## 8. 配置需求

配置名称在概要设计阶段最终确定。V1 至少需要表达以下配置：

| 配置能力 | 说明 |
|---|---|
| TDE 默认策略 | 新数据库默认启用或关闭 |
| Key Provider | Provider 类型和非敏感连接配置 |
| 数据加密算法 | 指定支持的页加密算法 |
| WAL 加密 | 是否开启；生产模式下应与数据 TDE 联动 |
| 临时文件加密 | TDE 启用时默认强制开启 |
| 密钥缓存策略 | 缓存大小、有效期或刷新方式 |
| 转换并发度 | 后台加密、解密或重加密 worker 数量 |
| 转换限速 | I/O 或带宽限制 |
| Provider 超时 | 连接、读取和重试策略 |

安全相关配置的默认值必须遵循失败关闭原则。不得因为 Provider 暂时不可用而自动改为明文写入。

## 9. 加密算法需求

**TDE-CRYPTO-001** V1 至少支持一种经国家或行业认可、适合固定页面加密的算法套件。

**TDE-CRYPTO-002** 算法实现应通过统一 Crypto Provider 接口调用，不允许业务路径散落调用具体算法函数。

**TDE-CRYPTO-003** 算法标识、参数、IV/nonce 派生规则、认证标签格式及密钥版本必须可版本化。

**TDE-CRYPTO-004** 如果使用 AES-XTS，应明确其仅提供保密性，并结合页面校验或独立认证机制处理数据损坏检测。

**TDE-CRYPTO-005** 如果使用 AES-GCM 等 AEAD 算法，必须证明同一密钥下 nonce 唯一，并解决页面重复覆盖、崩溃恢复和认证标签持久化问题。

**TDE-CRYPTO-006** 禁止使用 ECB 加密数据页，禁止固定 IV，禁止将物理块号直接作为全部 nonce 而不处理文件重用和页面重写问题。

**TDE-CRYPTO-007** 应支持算法敏捷性，使新算法上线后可以读取旧格式并逐步迁移。

## 10. 兼容性需求

TDE 不得改变以下数据库行为：

- SQL 语法和客户端协议；
- 表、列和索引的逻辑结构；
- MVCC、锁和事务隔离语义；
- Planner 代价模型与执行计划正确性；
- B-tree、Hash、GiST、GIN、SP-GiST、BRIN 等索引的逻辑行为；
- VACUUM、ANALYZE、CLUSTER、REINDEX 和表重写；
- Checkpoint、崩溃恢复和热备查询；
- 表空间移动和数据库复制。

以下工具必须识别加密格式并给出明确行为：

- `pg_basebackup`；
- `pg_rewind`；
- `pg_checksums`；
- `pg_waldump`/产品对应工具；
- `pg_upgrade`/产品对应工具；
- 页面检查、备份校验和控制数据查看工具。

工具不具备密钥时不得把密文误报为普通页面损坏；应报告“页面已加密且密钥不可用”或等价信息。

## 11. 性能与资源需求

以下为 V1 验收目标，最终阈值可在基准测试后调整：

| 指标 | 目标 |
|---|---|
| OLTP 吞吐下降 | 相对同配置未加密基线不超过 10% |
| 顺序扫描吞吐下降 | 不超过 15% |
| WAL 生成路径吞吐下降 | 不超过 10% |
| 单页随机读 P95 延迟增长 | 不超过 15% |
| 数据文件空间膨胀 | 不超过 2%，不含密钥元数据固定开销 |
| Key Provider 暂时不可用 | 已缓存有效 DEK 时按策略继续；需新密钥时失败关闭 |

性能测试必须分别覆盖：

- AES-NI/硬件加速开启和关闭；
- 单连接和高并发；
- OLTP、OLAP、批量装载和 VACUUM；
- WAL 密集型负载；
- 主备复制；
- 后台加密或轮换扫描与前台业务并行。

## 12. 可用性与故障处理

**TDE-NFR-001** TDE 不得降低数据库原有持久性保证。

**TDE-NFR-002** 在页面写入、WAL 写入、密钥切换和状态变更的任意步骤发生进程崩溃或断电后，数据库必须能够确定数据所用密钥版本并恢复。

**TDE-NFR-003** 单个页面解密或认证失败不得返回未经验证的数据。

**TDE-NFR-004** 密钥服务短时故障不得导致密钥材料泄露、明文降级或错误轮换。

**TDE-NFR-005** Provider 恢复后应支持自动重试或管理员触发恢复，不要求重建数据库。

**TDE-NFR-006** 所有持久化加密元数据必须具备校验、版本控制和必要的冗余或恢复机制。

## 13. 升级与迁移需求

1. 未启用 TDE 的旧集群升级后应保持原有明文状态，不自动改变存储格式。
2. 新版本必须能够识别旧版本 TDE 格式；不支持时应在启动或升级预检查阶段阻止升级。
3. `pg_upgrade` 或产品升级工具必须迁移加密元数据，并验证所有 Key ID/Version 可用。
4. 明文到密文、密文到明文及不同算法/DEK 间迁移必须具备可恢复状态机。
5. 迁移期间产生的新页面和新 WAL 不得出现策略不确定状态。
6. 降级前必须验证目标版本能够识别当前加密格式和密钥版本。

## 14. 管理接口初步需求

具体 SQL 语法不是本需求文档的最终约束，但功能上应提供等价能力：

```sql
-- 查看状态
SELECT * FROM system_tde_status;

-- 启用数据库 TDE
ALTER DATABASE dbname SET ENCRYPTION ON;

-- 暂停/继续后台扫描
ALTER DATABASE dbname SET ENCRYPTION SUSPEND;
ALTER DATABASE dbname SET ENCRYPTION RESUME;

-- KEK 轮换或 DEK 轮换
ALTER DATABASE dbname ROTATE ENCRYPTION KEY;

-- 禁用 TDE并后台解密
ALTER DATABASE dbname SET ENCRYPTION OFF;
```

管理命令必须支持幂等或明确报告当前状态，并能区分“提交成功但后台处理中”与“操作完成”。

## 15. 验收标准

### 15.1 功能验收

1. 新建加密数据库后，使用十六进制或字符串工具扫描数据文件，不能找到测试明文特征串。
2. Heap、所有索引、TOAST、FSM、VM、临时文件、WAL 和归档 WAL 分别通过明文泄露检查。
3. 正常 SQL、索引查询、事务、触发器、分区表和并行查询结果与未加密数据库一致。
4. 加密数据库可完成重启、崩溃恢复、PITR、基础备份恢复和主备切换。
5. 缺少或使用错误密钥时，数据库无法读取加密数据，并产生明确且无敏感信息的错误。
6. KEK 轮换后无需全量重写数据，旧备份在提供历史密钥时仍可恢复。
7. 转换或轮换过程中随机终止进程、断电模拟后，数据库可恢复并继续任务。
8. 加密状态、进度、密钥版本和审计记录正确。

### 15.2 安全验收

1. 数据目录、WAL 归档和物理备份中不存在约定测试明文。
2. 配置文件、日志、审计、状态视图、进程命令行中不存在明文密钥。
3. 页面复制到其他块、其他关系或其他数据库时能够被拒绝或检测。
4. 密文或认证标签被修改后，读取必须失败。
5. 重复页面写入不会违反所选算法的 IV/nonce 安全约束。
6. 未授权用户不能执行 TDE 管理和密钥操作。

### 15.3 性能验收

使用固定硬件、相同数据集和相同配置，对未启用 TDE、仅数据页加密、数据页与 WAL 全加密三种模式进行对比，达到第 11 章目标或形成经评审接受的偏差说明。

## 16. 测试范围

测试至少包括：

- 单元测试：算法适配、密钥包装、页面身份、格式解析、错误处理；
- 回归测试：DDL、DML、索引、TOAST、分区、VACUUM、表重写；
- 故障注入：I/O 失败、Provider 超时、错误密钥、页面损坏、各阶段崩溃；
- 恢复测试：crash recovery、归档恢复、PITR、备份恢复；
- 复制测试：同步/异步、级联、延迟备库、主备切换；
- 轮换测试：KEK、DEK、WAL DEK及多版本共存；
- 升级测试：明文集群升级、加密集群升级和失败回滚；
- 安全测试：静态文件扫描、密钥泄露检查、权限绕过、篡改检测；
- 性能测试：TPC-C 类、TPC-H 类、COPY、索引构建、VACUUM和重放。

## 17. 建议的版本拆分

### 阶段一：技术验证

- 单一数据库或表空间加密域；
- 单一算法；
- 本地测试 Key Provider；
- 数据页 I/O 加解密；
- 页面格式和崩溃恢复验证；
- 明文泄露扫描工具。

### 阶段二：V1 产品化

- 生产级 Key Provider；
- WAL 与临时文件加密；
- KEK 轮换；
- 物理备份、恢复和流复制；
- 状态、审计、权限和后台转换；
- 工具链适配和性能优化。

### 阶段三：增强

- 在线 DEK 全量重加密；
- 多 Provider 和 KMS/HSM；
- 算法迁移；
- 更细粒度表空间策略；
- 认证加密和更强的防篡改能力。

## 18. 待决策事项

以下事项必须在概要设计评审前确定：

1. V1 加密粒度采用数据库级、表空间级还是两者同时支持。
2. 首选算法采用 AES-256-XTS、SM4-XTS，还是支持 AEAD 页面格式。
3. 页面 Key Version、IV/nonce、认证标签等元数据存放位置。
4. PostgreSQL 页面校验和是在明文上还是密文上计算，以及离线工具如何校验。
5. WAL 是按 WAL page、WAL segment 还是 record block 加密。
6. V1 生产级 Key Provider 的类型与高可用要求。
7. 主备节点密钥分发、密钥切换屏障及备库不可用时的策略。
8. 临时文件、逻辑解码 spill 和扩展自建文件的统一接入机制。
9. 是否允许仅加密数据文件但不加密 WAL；建议生产模式禁止这种不完整配置。
10. DEK 轮换是 V1 必须项还是仅提供新写入切换和历史版本读取。
11. 是否需要国密合规，以及 SM4 算法模式和密码模块认证要求。
12. 物理备份恢复时是复用原 DEK，还是使用传输密钥重新包装 DEK。

## 19. 风险

| 风险 | 影响 | 初步措施 |
|---|---|---|
| I/O 路径存在旁路 | 产生明文文件 | 建立全量文件清单和自动明文扫描测试 |
| nonce/IV 重用 | 破坏密码安全 | 设计评审和密码专家审查，加入重复检测测试 |
| 密钥丢失 | 数据永久不可恢复 | 强制密钥备份、恢复预检和多版本保留 |
| WAL 与数据密钥切换不同步 | 主备或恢复失败 | WAL 持久化切换记录和复制屏障 |
| 加密影响性能 | 吞吐和延迟退化 | 使用硬件加速、批量处理、缓存 DEK和基准测试 |
| 工具不识别加密页 | 运维误判或破坏数据 | 所有物理工具增加格式识别和密钥接口 |
| 后台转换中断 | 明密文混合且状态不明 | 页面级版本标识、可恢复状态机和幂等扫描 |
| Provider 故障 | 数据库无法启动或写入 | 高可用 Provider、受控缓存、清晰故障策略 |

## 20. 交付物

TDE V1 应至少交付：

1. 需求文档、概要设计、详细设计和威胁模型；
2. 数据页、WAL、临时文件加密实现；
3. Key Provider 接口及至少一个生产级实现；
4. 管理 SQL、状态视图、权限和审计；
5. 后台加密、解密及轮换任务；
6. 备份、恢复、复制和工具链适配；
7. 单元、回归、故障、恢复、安全和性能测试；
8. 管理员手册、密钥备份恢复手册和故障处置手册；
9. 密码设计与实现安全评审报告。
